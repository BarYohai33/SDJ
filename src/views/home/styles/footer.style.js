// Style.js

const text = {
	fontSize: 20,
	paddingTop: 10,
	color: "#FFF",
};

const iconText = {
	paddingTop: 1,
	color: "#FFF",
};

const button = {
	flexDirection: 'row',
	alignItems: 'center'
};

const image = {
	width: 100,
	height: 45
};

export default { text, iconText, button, image };